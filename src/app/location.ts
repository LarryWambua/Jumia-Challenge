import { Coordinates } from "./coordinates";

export class Location {
    constructor(
        street: string = '',
        city: string,
        state: string,
        postcode: string,
        coordinates: Coordinates
        ) {

        }
}

